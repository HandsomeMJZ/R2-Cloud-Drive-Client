# R2 Cloud Drive Client 技术文档

## 1. 技术栈

- Electron：桌面窗口、托盘、系统通知、IPC。
- electron-builder：Windows/macOS/Linux 打包，Windows 使用 NSIS。
- Node.js：本地文件扫描、上传下载流、配置文件读写。
- Capacitor：移动端资源与 Android 构建入口。
- Cloudflare R2 Drive API：文件管理、分片上传、分布式上传、同步目录记录。

## 2. 进程架构

### 主进程

入口：`src/main/main.js`

职责：

- 创建无边框主窗口。
- 创建系统托盘和右键菜单。
- 注册 IPC 接口。
- 维护上传、下载和同步任务状态。
- 调用 `R2DriveClient` 访问服务端。
- 调用 `BackupManager` 执行自动同步。
- 发送系统通知和任务栏进度。

### 预加载脚本

入口：`src/preload/preload.js`

职责：

- 使用 `contextBridge.exposeInMainWorld` 暴露安全 API。
- 渲染进程不能直接访问 Node.js 和 Electron 主进程能力。
- 所有文件系统、窗口、托盘、上传下载和同步行为都通过 IPC 调用。

### 渲染进程

入口：

- `src/renderer/index.html`
- `src/renderer/renderer.js`
- `src/renderer/styles.css`

职责：

- 渲染桌面端 UI。
- 维护当前视图、文件列表、传输列表、同步任务、同步弹窗等前端状态。
- 接收主进程事件：`transfer:event`、`backup:event`、`sync:update-event`。
- 将用户操作转换为 preload API 调用。

## 3. 配置模型

配置文件路径由 Electron `app.getPath('userData')` 决定，文件名为 `config.json`。

核心字段：

- `baseUrl`：服务端 API 基准地址。
- `sessionCookie`：登录态 Cookie。
- `downloadDir`：默认下载目录。
- `backupJobs`：自动同步任务数组。
- `backupIntervalMinutes`：自动同步间隔。
- `backupAutoStart`：开机自启动开关。
- `backupSyncClientId`：同步目录记录客户端 ID。
- `backupSyncPromptDismissed`：跨端同步目录提示是否已忽略。
- `closeBehavior`：关闭窗口行为。
- `minimizeBehavior`：最小化按钮行为。
- `startHiddenToTray`：启动后隐藏到托盘。
- `autoLaunch`：通用开机自启动开关。
- `uploadBatchNotify`：上传批次完成通知开关。
- `downloadBatchNotify`：下载批次完成通知开关。
- `autoSyncEnabled`：多端自动同步开关。

`apiClient.js` 负责读取、规范化、写入这些字段。

## 4. Windows 安装器

打包配置位于 `package.json` 的 `build` 字段。

Windows 目标：

```json
"win": {
  "icon": "assets/icons/icon.ico",
  "target": ["nsis", "portable"]
}
```

NSIS 配置：

```json
"nsis": {
  "oneClick": false,
  "allowToChangeInstallationDirectory": true,
  "createDesktopShortcut": true,
  "createStartMenuShortcut": true,
  "installerIcon": "assets/icons/icon.ico",
  "uninstallerIcon": "assets/icons/icon.ico",
  "shortcutName": "R2 Cloud Drive"
}
```

说明：

- `oneClick: false` 启用安装向导。
- `allowToChangeInstallationDirectory: true` 显示安装路径选择。
- `installerIcon` 和 `uninstallerIcon` 保证安装/卸载程序图标一致。

## 5. 图标策略

### 应用图标

主进程使用 `nativeIcon()` 统一加载图标：

1. 优先使用调用方指定图标。
2. 失败后依次回退到 `icon.png`、`icon.ico`、`tray.png`。
3. 托盘图标固定缩放为 16x16，避免 Windows 托盘显示异常。

Windows 任务栏图标使用固定 AppUserModelID：

```js
app.setAppUserModelId('io.qzz.r2drive.client');
```

该 ID 与 `electron-builder` 的 `appId` 保持一致，用于提高快捷方式、通知和任务栏图标匹配稳定性。

### 文件类型图标

文件列表和同步弹窗文件图标使用 CSS 绘制的 `.file-badge`：

- 不依赖 Google Material Icons 字体。
- 离线环境不会显示成破碎图标或 ligature 文本。
- 根据扩展名映射为 folder、image、video、audio、archive、pdf、document、sheet、presentation、code、executable、database、generic。

## 6. 自动同步设计

核心文件：`src/main/backupManager.js`

### 任务结构

每个任务包含：

- `id`：任务 ID。
- `name`：显示名称。
- `localPath`：本地目录。
- `remotePath`：远端目录。
- `kind`：`folder` 或 `album`。
- `mediaOnly`：是否仅同步媒体文件。
- `enabled`：是否启用。
- `createdAt`、`lastRunAt`、`lastStatus`、`lastMessage`、`lastSyncCheck`。

### 调度流程

1. 应用启动后创建 `BackupManager`。
2. `start()` 根据已有启用任务启动定时器。
3. 添加/删除/启停任务或修改间隔后调用 `restartTimer()`。
4. `runAll()` 使用 `runAllPromise` 防止全量同步重复并发。
5. 单个任务使用 `runningJobs` 防止同一任务重复执行。

### 扫描流程

1. 检查本地目录是否存在。
2. 创建远端目录。
3. 拉取远端当前目录文件列表。
4. 并发扫描子目录。
5. 并发分析文件是否需要上传。

扫描阶段会容错处理不可访问子目录和读取失败文件，尽量完成其他文件同步。

### 文件上传判断

文件会被跳过的情况：

- 相册任务中不是媒体扩展名。
- 文件名匹配 `.tmp`、`.part`、`.crdownload` 等临时文件。
- 文件修改时间距离当前时间小于 2 秒，可能仍在写入。
- 远端文件上传时间不早于本地修改时间，且文件大小一致。
- 上传前本地文件大小或修改时间发生变化。

这样可以降低重复上传、半文件上传和正在下载文件被同步的风险。

## 7. 上传下载设计

核心文件：`src/main/apiClient.js`

上传策略：

- 小文件使用 `/api/upload` 普通上传。
- 大文件优先尝试分布式上传。
- 分布式初始化不支持或返回可回退错误时，使用 R2 Multipart。

下载策略：

- 使用 `downloadToFile()`。
- 支持 Range 重试。
- 下载失败后删除不完整目标文件。

主进程会将上传下载转换为统一的 transfer 事件，渲染层用于显示传输列表和悬浮状态。

## 8. 多端同步

多端同步基于同步任务的远端目录。

### 目录同步

- `backup:remote-dirs-get` 获取服务端保存的同步目录。
- `backup:push-dirs-to-remote` 将本机同步目录推送到服务端。
- `backup:sync-remote-folders` 选择本地根目录后下载远端目录，并创建本地同步任务。

### 文件更新同步

主进程定时执行：

1. 遍历启用的同步任务。
2. 递归扫描远端目录。
3. 使用 `lastSyncCheck` 判断是否有远端更新。
4. 如果 `autoSyncEnabled` 为 true，直接下载更新文件到本地。
5. 如果未启用自动同步，发送 `sync:update-event` 让渲染进程弹窗确认。

点击“以后都同步”后：

1. 设置 `autoSyncEnabled`。
2. 调用 `sync:apply-file-updates` 应用本次弹窗中的更新。
3. 后续轮询自动同步，不再仅提示。

下载更新文件时先写入临时文件，成功后再替换目标文件，避免同步失败留下半文件。

## 9. IPC 接口分组

主要接口：

- `config:*`：基础配置、连接测试、下载目录、开机自启。
- `backup:*`：同步配置、添加目录、启停任务、手动同步、同步目录记录。
- `drive:*`：文件列表、上传下载、新建目录、删除、重命名、预览、节点管理。
- `clipboard:*`：服务端剪贴板。
- `window:*`：最小化、隐藏到托盘、最大化、关闭。
- `sync:*`：远端更新检查、自动同步开关、应用同步更新。
- `reset:*`：清理本地配置并重启。

## 10. 构建与验证

开发检查：

```powershell
npm.cmd run check
```

Windows 打包：

```powershell
npm.cmd run dist:win
```

建议发布前验证：

- 安装器能选择安装目录。
- 桌面快捷方式启动正常。
- 任务栏、托盘和通知图标正常。
- 文件列表、同步弹窗文件图标正常。
- 添加同步目录后手动同步成功。
- 修改文件后下一轮同步只上传变更文件。
- 多端同步弹窗点击“以后都同步”后能应用更新。

## 11. 已知边界

- 远端文件是否更新主要依赖服务端返回的 `uploaded` 时间和文件大小。
- 若服务端不返回稳定的文件大小，客户端会退化为只按上传时间判断。
- 自动同步不会删除远端多余文件，当前语义是增量上传/覆盖。
- 相册同步仅按扩展名判断媒体类型。
- 自定义品牌 HTML/CSS 应仅在可信环境使用。
